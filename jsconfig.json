{
  "compilerOptions": {
    // This must be specified if "paths" is set
    "baseUrl": ".",
    "jsx": "preserve",
    // Relative to "baseUrl"
    "paths": {
      "*": [
        "*",
        "app/*"
      ]
    }
  }
}